# 카카오 캐러셀 디자인 명세서

**규격**: 750 × 1000 px (캐러셀)
**최종 업데이트**: 2026-05-15

---

## 📐 캐러셀 기본 규격

### 사이즈
- **너비**: 750px
- **높이**: 1000px
- **카드 종류**: 표지(cover), 내지(inner), 아웃트로(outro)

### 폰트 (시스템)
- **폰트 패밀리**: Pretendard
- **소스**: Google Fonts CDN

### 색상 시스템 (미리보기 카드 내부)

미리보기 카드에서 사용되는 색상은 빌더 UI 시스템 토큰(--y100~y300)과 별개.
미리보기 색상은 카카오 브랜드 가이드를 따름.

| 용도 | 색상 코드 | 비고 |
|------|----------|------|
| 카카오 브랜드 노랑 | `#FEE500` | 버튼 기본값 |
| 카카오 브라운 | `#3C1E1E` | 노랑 배경 위 텍스트 |
| 강조 빨강 (할인) | `#EF3C2C` | 할인율 표시 |
| 다크 텍스트 | `#111` | 헤드라인 |
| 서브 텍스트 | `#5F5F5F` | 본문 |
| 힌트 텍스트 | `#C0C0BC` | 비활성 |
| 라이트 배경 | `#F5F5F5` | 카드 배경 |
| 화이트 배경 | `#FFFFFF` | 내부 박스 |

---

## 🎨 카드 A — 브랜드형 (표지)

> **목적**: 브랜드 이미지로 시선을 끌고, 메인 메시지와 혜택을 전달
> **사용 위치**: 표지 (Cover)
> **변형**: 연출컷 / 누끼컷 (이미지 타입에 따라 레이아웃 분기)

### 레이아웃 — 공통

```
┌─────────────────────────────┐  ← top: 0
│   서브텍스트 (100px ↓)       │
│   메인텍스트 1줄            │
│   메인텍스트 2줄 (선택)     │
│                             │
│         이미지 영역          │
│                             │
│   ┌───혜택 박스───┐          │  ← bottom: 108px
│   │ 라벨 │ 값 │  │          │
│   └────────────┘          │
│ ━━━━━ 버튼 영역 ━━━━━━━━     │  ← height: 108px
└─────────────────────────────┘
```

#### 텍스트 영역 (헤드라인 + 서브)
- 위치: 상단 절대배치 `top: 0`
- 패딩: `padding: 100px 60px 0`
- 정렬: 가운데 정렬 (`text-align: center`)
- z-index: 3

#### 이미지 영역
- **연출컷**: `width: 750px`, `height: 1000px` (카드 전체) + 상단 520px 그라디언트 오버레이
- **누끼컷**: 텍스트 아래부터 혜택 박스 위까지 동적 계산 영역
  - `top = 100 + 48 + (헤드라인2 있으면 154 / 없으면 77) + 40`
  - `height = (1000 - 108 - 166) - 40 - top`
- z-index: 1
- `object-fit`: 연출컷 `cover` / 누끼컷 `contain`
- 변환: `transform: translate(X, Y) scale(S)` (위치 조정 적용)

#### 혜택 박스 영역
- 위치: `bottom: 108px`, 좌우 패딩 30px
- 배경: 흰색 라운드 박스 (`border-radius: 20px`)
- 내부 패딩: `36px 40px`
- 표시 조건: 혜택 개수 ≠ '없음' 일 때만

#### 버튼 영역
- 위치: 하단 `bottom: 0`
- 크기: `width: 750px × height: 108px`
- 배경: 사용자 지정 (`btnColor`, 기본값 `#2f8de2`)
- 정렬: 가운데 (텍스트 + 화살표 SVG)
- z-index: 3

---

### 타이포그래피

| 요소 | 폰트 크기 | 굵기 | line-height | letter-spacing | 색상 | maxlength |
|------|----------|------|-------------|----------------|------|-----------|
| 서브텍스트 (sub) | 40px | 400 | - | -0.36px | 자동 분기 ※1 | 16 |
| 메인텍스트 1줄 (headline) | 70px | 700 | 70px | -1.4px | 자동 분기 ※2 | 10 |
| 메인텍스트 2줄 (headline2) | 70px | 700 | 70px | -1.4px | 자동 분기 ※2 | 10 |
| 혜택 라벨 (2개 모드) | 30px | 400 | - | - | `#5f5f5f` | 10 |
| 혜택 라벨 (3개 모드) | 30px | 400 | - | - | `#5f5f5f` | 7 |
| 혜택 값 (2개 모드) | 36px | 600 | - | - | 자동 분기 ※3 | 8 |
| 혜택 값 (3개 모드) | 36px | 600 | - | - | 자동 분기 ※3 | 5 |
| 버튼 텍스트 (btnText) | 36px | 600 | - | -0.36px | 자동 분기 ※4 | 15 |

**자동 분기 로직**:
- ※1 **서브텍스트 색상**: 배경 밝기 `< 120` → `rgba(255,255,255,0.7)` / `≥ 120` → `#393939`
- ※2 **메인텍스트 색상**: 배경 밝기 `< 120` → `#fff` / `≥ 120` → `#111`
- ※3 **혜택 값 색상**: 버튼 밝기 `≥ 200` → `#111` / `< 200` → 버튼 컬러
- ※4 **버튼 텍스트 색상**: 버튼 밝기 `≥ 168` → `#000000` / `< 168` → `white`
- 밝기 계산식: `(R×299 + G×587 + B×114) / 1000`

---

### 색상

#### 연출컷
- **배경**: `#F0F0EE` (고정)
- **그라디언트 오버레이** (상단 520px):
  - 컬러: `_gradColor` 또는 자동 추출 `_autoGradColor` 또는 기본값
  - 형태: `linear-gradient(180deg, color 0% → color 30% → 0.85 45% → 0.5 60% → 0.15 75% → 0% 100%)`
- **버튼**: `btnColor` (기본 `#2f8de2`)
- **혜택 박스 배경**: `rgba(255,255,255,1)` 흰색
- **혜택 박스 구분선**: `#E8E8E4`, 두께 1px, 높이 70px

#### 누끼컷
- **배경**: `bgColor` (기본 `#1A1A18`, 누끼 전환 시 `#ffffff`로 초기화)
- **버튼**: `btnColor` (기본 `#2f8de2`)
- **혜택 박스 배경**: `rgba(255,255,255,1)` 흰색
- **혜택 박스 구분선**: `#E8E8E4`, 두께 1px, 높이 70px

---

### 인터랙션

| 기능 | 지원 | 비고 |
|------|------|------|
| 이미지 위치 조정 | ✅ | 드래그/핸들/휠 (X, Y, Scale) |
| 자동 컬러 추출 | ✅ | 누끼: `_bgColorAuto`, `_btnColorAuto` / 연출: `_gradColorAuto`, `_btnColorAuto` |
| 스포이드 (이미지 컬러 추출) | ✅ | 파일 업로드 또는 자동 모드일 때만 활성 |
| 혜택 개수 조정 | ✅ | 없음 / 2개 / 3개 |
| 버튼 화살표 토글 | ✅ | `btnArrow` (on/off) |
| 텍스트 자동 색상 분기 | ✅ | 배경/버튼 밝기 기반 |

#### 자동 컬러 추출 상세
- **누끼컷**:
  - 배경 컬러: 이미지 중앙 영역(40~60%)에서 평균 색상 추출 → `_autoBgColor`
  - 버튼 컬러: 추출 색상에 채도 +0.25, 명도 -0.25 적용 (`calcBtnColor()`)
- **연출컷**:
  - 그라디언트 컬러: 16×16 다운샘플 → 가장 많이 등장한 색상 (밝기 20~230 범위)
  - 버튼 컬러: 동일 베이스 색상에 `calcBtnColor()` 적용

---

### 변형: 연출컷 vs 누끼컷

| 항목 | 연출컷 | 누끼컷 |
|------|--------|--------|
| 배경 | `#F0F0EE` 고정 | 사용자 지정 (`bgColor`) |
| 그라디언트 오버레이 | ✅ 상단 520px | ❌ 없음 |
| 이미지 fit | `cover` | `contain` |
| 이미지 영역 | 카드 전체 | 텍스트 아래 ~ 혜택 박스 위 |
| 자동 추출 컬러 | 그라디언트 + 버튼 | 배경 + 버튼 |
| 텍스트 색상 기준 | 그라디언트 색상 밝기 | 배경 색상 밝기 |

---

### 데이터 구조 (default)

```javascript
{
  imageType: '연출컷',          // '연출컷' | '누끼컷'
  benefitCount: '2개',          // '없음' | '2개' | '3개'
  sub: '프리미엄 컬렉션',
  headline: '최대 56%',
  headline2: '할인',
  benefit1Label: '혜택 1', benefit1Value: '무료배송',
  benefit2Label: '혜택 2', benefit2Value: '쿠폰 증정',
  benefit3Label: '혜택 3', benefit3Value: '단독 할인',
  btnText: '할인받고 구매하기',
  btnArrow: 'on',
  bgColor: '#1A1A18',           // 누끼컷 배경
  btnColor: '#2f8de2',          // 버튼 컬러
  _imgX: 0, _imgY: 0, _imgScale: 1,
  _imgEditing: false,
  _gradColor: '',               // 연출컷 그라디언트 (수동)
  _autoGradColor: '',           // 연출컷 그라디언트 (자동 추출값)
  _btnColorAuto: true           // 버튼 자동 추출 플래그
}
```

#### 컬러 자동 플래그 키
- `_bgColorAuto` (누끼컷 전용): `false`면 수동, 기본 `true`
- `_gradColorAuto` (연출컷 전용): `true`면 자동
- `_btnColorAuto` (공통): `true`면 자동

---

## 🎨 카드 B — 가격강조형 (표지)

> **목적**: 제품 이미지와 가격을 강조하여 구매 욕구 자극
> **사용 위치**: 표지 (Cover)
> **변형**: 상품 1개 / 상품 2개 (다른 레이아웃)

### 레이아웃 — 상품 1개일 때

```
┌─────────────────────────────┐
│       헤드라인 (80px ↓)      │  ← top: 0, padding-top: 80px
│                             │
│  ┌─── 흰색 라운드 박스 ───┐  │  ← top: 190px, left/right: 40px
│  │  날짜 │ 시간 (선택)     │  │  ← top: 24px (in box)
│  │ ┌──── 이미지 박스 ────┐ │  │
│  │ │                      │ │  │  ← 회색 배경, 라운드
│  │ │       이미지         │ │  │
│  │ │                      │ │  │
│  │ └──────────────────────┘ │  │
│  │  브랜드명                │  │
│  │  제품명                  │  │
│  │  -할인%  판매가  원가    │  │
│  └──────────────────────────┘  │  ← bottom: 162px
│ ━━━━━━ 버튼 영역 ━━━━━━━━━     │  ← bottom: 40px, height: 100px
└─────────────────────────────┘
```

#### 헤드라인 영역
- 위치: `top: 0`, `padding: 80px 60px 0`
- 너비: 750px
- 정렬: 가운데 (`text-align: center`)
- z-index: 2

#### 상품 박스 (흰색 라운드 박스)
- 위치: `top: 190px`, `left: 40px`, `right: 40px`, `bottom: 162px`
- 배경: 흰색
- 모서리: `border-radius: 20px`
- overflow: hidden
- z-index: 1

#### 날짜/시간 영역 (선택)
- 위치 (박스 내부): `top: 24px`, `left/right: 24px`
- 표시: `showDate: 'on'`일 때만
- 정렬: `justify-content: space-between` (좌: 날짜, 우: 시간)
- z-index: 2

#### 이미지 영역
- 위치 (박스 내부): `top: 70px` (날짜 있을 때) 또는 `20px` (없을 때)
- 좌우: `left/right: 20px`
- 하단: `bottom: 186px`
- 배경: `#f5f5f5`
- 모서리: `border-radius: 20px`
- overflow: hidden
- `object-fit: cover`
- 변환: `transform: translate(X, Y) scale(S)` (위치 조정 적용)

#### 가격 정보 영역
- 위치 (박스 내부): `bottom: 28px`, `left/right: 24px`
- 구성: 브랜드명 → 제품명 → (할인율 + 판매가 + 원가) 세로 배치
- gap: 12px

#### 버튼 영역
- 위치: `bottom: 40px`, `left/right: 40px`
- 크기: `height: 100px`
- 모서리: `border-radius: 20px`
- 배경: `btnColor` (기본 `#00A1FF`)
- 정렬: 가운데 (텍스트 + 화살표 SVG)
- z-index: 2

---

### 레이아웃 — 상품 2개일 때

```
┌─────────────────────────────┐
│       헤드라인 (80px ↓)      │  ← top: 0
│                             │
│  ┌────── 상품 카드 1 ──────┐  │  ← top: 200px
│  │ 날짜 │ 시간 (선택)      │  │
│  │ ━━━━━ 구분선 ━━━━━━     │  │
│  │ ┌─img──┐  -할인%↗      │  │  ← 할인 뱃지 (top-left)
│  │ │      │  브랜드명      │  │
│  │ │      │  제품명        │  │
│  │ │      │  판매가  원가  │  │
│  │ └──────┘                │  │
│  └─────────────────────────┘  │
│  ┌────── 상품 카드 2 ──────┐  │  ← gap: 16px
│  │       (동일 구조)       │  │
│  └─────────────────────────┘  │  ← bottom: 160px
│ ━━━━━━ 버튼 영역 ━━━━━━━━━     │  ← bottom: 40px
└─────────────────────────────┘
```

#### 상품 카드 컨테이너
- 위치: `top: 200px`, `left/right: 40px`, `bottom: 160px`
- 레이아웃: `flex-direction: column`, `gap: 16px`
- 각 카드: `flex: 1` (균등 분배)

#### 개별 상품 카드
- 배경: 흰색
- 모서리: `border-radius: 20px`
- 패딩: `20px`
- 레이아웃: `flex-direction: column`, `gap: 16px`

#### 카드 내부: 날짜/시간 (선택)
- 표시: `showDate: 'on'`일 때
- 정렬: `space-between`
- 구분선: `height: 1px`, `background: #f0f0f0`

#### 카드 내부: 이미지 + 정보 (가로 배치)
- 레이아웃: `flex`, `gap: 20px`
- 이미지 영역: `width: 240px`, `border-radius: 20px`, 회색 배경
- 정보 영역: `flex: 1`, 세로 배치

#### 할인율 배지 (상품 2개 전용)
- 위치: 이미지 위 `top: 0`, `left: 19px` (absolute)
- 배경: `#ef3c2c` (강조 빨강)
- 패딩: `10px 10px 10px 12px`
- 모서리: `border-radius: 0 0 10px 10px` (하단만 둥글게)
- 텍스트: 흰색

---

### 타이포그래피

| 요소 | 폰트 크기 | 굵기 | line-height | letter-spacing | 색상 | maxlength |
|------|----------|------|-------------|----------------|------|-----------|
| 헤드라인 (headline) | 70px | 700 | 70px | -1.4px | `#111` | 10 |
| 날짜/시간 | 26px | 400~500 | - | - | `#5f5f5f` | 10 |
| 브랜드명 (1개) | 30px | 500 | - | - | `#111` | 15 |
| 제품명 (1개) | 30px | 400 | - | - | `#5f5f5f` | 20 |
| 브랜드명 (2개) | 30px | 500 | - | - | `#111` | 12 |
| 제품명 (2개) | 30px | 400 | - | - | `#5f5f5f` | 12 |
| 할인율 (1개, inline) | 40px | 600 | - | - | `#ef3c2c` | 2 (숫자만) |
| 판매가 (1개) | 40px | 600 | - | - | `#111` | 10 |
| 원가 (취소선) | 30px | 400 | - | - | `#cfcfcf` (line-through) | 10 |
| 할인율 (2개, 배지) | 30px (숫자) + 22px (%) | 600 | 30px / 26px | - | white | 2 |
| 판매가 (2개) | 40px | 600 | - | - | `#111` | 10 |
| 버튼 텍스트 (btnText) | 36px | 600 | - | -0.36px | white | 15 |

---

### 색상

- **배경 (카드 전체)**: `#f5f5f5` (고정)
- **상품 박스 배경**: 흰색
- **이미지 박스 배경**: `#f5f5f5` (이미지 없을 때)
- **할인율 강조**: `#ef3c2c` (빨강)
- **취소선 원가 색상**: `#cfcfcf`
- **구분선 (2개 모드)**: `#f0f0f0`, 두께 1px
- **버튼 컬러**: `btnColor` (기본 `#00A1FF`, 사용자 지정)
- **버튼 텍스트**: 흰색 (고정 — 자동 분기 없음)
- **헤드라인 컬러**: `#111` (고정 — 자동 분기 없음)

---

### 인터랙션

| 기능 | 지원 | 비고 |
|------|------|------|
| 이미지 위치 조정 | ✅ | 이미지 1, 2 각각 가능 |
| 자동 컬러 추출 | ❌ | `_btnColorAuto: false` (기본 비활성) |
| 화살표 토글 | ✅ | `btnArrow` (on/off) |
| 날짜 표시 토글 | ✅ | `showDate` (on/off) |
| 상품 개수 조정 | ✅ | 1개 / 2개 (레이아웃 변경) |
| 텍스트 자동 색상 분기 | ❌ | 모든 색상 고정 |

#### 입력 검증
- **할인율**: 숫자만 입력 (정규식 `[^0-9]` 제거), 최대 2자리 + `%` 자동 부착
- **판매가/원가**: 숫자와 콤마만 입력 (정규식 `[^0-9,]` 제거), 최대 10자 + `원` 자동 부착
- **원가 빈값**: 입력 없으면 `원` 부착 안 함 (표시 자체 안 됨)

---

### 변형: 상품 1개 vs 2개

| 항목 | 상품 1개 | 상품 2개 |
|------|----------|----------|
| 상품 박스 위치 | `top: 190px ~ bottom: 162px` | `top: 200px ~ bottom: 160px` |
| 카드 개수 | 1개 (큰 박스) | 2개 (세로 배치, flex:1 균등) |
| 이미지 영역 | 박스 전체 너비 | `width: 240px` (좌측) |
| 가격 정보 위치 | 이미지 아래 (세로) | 이미지 우측 (가로) |
| 할인율 표시 | 인라인 (판매가 옆) | 별도 배지 (이미지 위) |
| 할인율 색상 | `#ef3c2c` 텍스트 | `#ef3c2c` 배경 + 흰색 텍스트 |
| 브랜드명 maxlength | 15 | 12 |
| 제품명 maxlength | 20 | 12 |
| 날짜/시간 위치 | 박스 상단 (공통) | 각 카드 상단 (개별) |
| 날짜 데이터 | `date`, `time` | `product1Date/Time`, `product2Date/Time` |

---

### 데이터 구조 (default)

```javascript
{
  productCount: '1개',           // '1개' | '2개'
  headline: '반값딜 알림 도착',
  showDate: 'off',               // 'on' | 'off'
  date: '8/22(금)',              // 상품 1 날짜 (1개 모드 공통)
  time: '오전 9시',              // 상품 1 시간
  product1Date: '8/22(금)',      // 상품 1 날짜 (2개 모드)
  product1Time: '오전 9시',
  product2Date: '8/22(금)',      // 상품 2 날짜 (2개 모드)
  product2Time: '오전 9시',
  product1Brand: '브랜드명',
  product1Name: '제품명',
  product1Rate: '50%',           // 할인율 (자동 % 부착)
  product1Price: '20,000원',     // 판매가 (자동 원 부착)
  product1Orig: '40,000원',      // 원가 (취소선, 자동 원 부착)
  product2Brand: '브랜드명',
  product2Name: '제품명',
  product2Rate: '50%',
  product2Price: '20,000원',
  product2Orig: '40,000원',
  btnText: '반값딜 입장하기',
  btnArrow: 'on',                // 'on' | 'off'
  btnColor: '#00A1FF',           // 버튼 컬러
  bgColor: '#f5f5f5',            // (사용 안 함, 카드 배경 고정)
  _img: '', _img2: '',           // 이미지 1, 2
  _imgX: 0, _imgY: 0, _imgScale: 1,
  _img2X: 0, _img2Y: 0, _img2Scale: 1,
  _imgEditing: false,
  _img2Editing: false,
  _btnColorAuto: false           // 자동 추출 비활성
}
```

---

## 🎨 카드 C — 혜택형 (내지)

> **목적**: 다양한 혜택을 배지로 시각화하여 강조
> **사용 위치**: 내지 (Inner)
> **변형**: 띠배너 6종 (브랜드형/오마트/오세일/오늘의딜/스오딜/없음) × 배지 0~3개

### 레이아웃

```
┌─────────────────────────────┐  ← top: 0
│ ━━━━━━ 띠배너 (104px) ━━━━━━ │  ← 띠배너 없으면 미표시
│                             │
│       ┌────배지1────┐ ←─────┤  ← top: 30px (배너 없음)
│       │   57%       │       │     또는 134px (배너 있음)
│       │  즉시할인   │       │  ← right: 30px
│       └─────────────┘       │
│       ┌────배지2────┐       │  ← gap: 10px
│       │   15%       │       │
│       │  상품쿠폰   │       │
│       └─────────────┘       │
│       ┌────배지3────┐       │  ← 결제사 배지
│       │   7%        │       │
│       │  카카오페이 │       │
│       └─────────────┘       │
│                             │
│         (이미지 배경)        │
│                             │
│   하단 유의사항 텍스트         │  ← bottom: 40px (선택)
└─────────────────────────────┘
```

#### 이미지 영역 (배경)
- 위치: 카드 전체 (`position: absolute; inset: 0`)
- `object-fit: cover`
- 변환: `transform: translate(X, Y) scale(S)` (위치 조정 적용)
- 빈 상태 시: `#E8E8E4` 배경 + "이미지" 텍스트
- z-index: 0 (최하위)

#### 띠배너 영역
- 위치: 상단 (`top: 0`)
- 높이: `104px` (고정)
- 좌우 패딩: 띠배너 타입별 다름 (`30px` 또는 `40px`)
- z-index: 2

#### 배지 영역
- 위치: 우측 상단
  - `top: 30px` (띠배너 "없음"일 때)
  - `top: 134px` (띠배너 있을 때, 띠배너 높이 104 + 30)
- `right: 30px`
- 레이아웃: `flex-direction: column`, `gap: 10px`
- z-index: 3 (최상위)

#### 하단 유의사항 영역
- 위치: `bottom: 40px`, `left/right: 0`
- 정렬: 가운데
- 표시: `showNotice: 'on'`일 때만
- z-index: 4

#### 하단 디밍 그라디언트 (유의사항 활성 시)
- 위치: 카드 전체 (`inset: 0`)
- 그라디언트: `linear-gradient(180deg, rgba(0,0,0,0) 71%, rgba(0,0,0,0.3) 100%)`
- 유의사항 가독성 보조
- z-index: 1
- 표시: `showNotice: 'on'`일 때만

---

### 띠배너 종류 (6가지)

| 타입 | 배경색 | 텍스트 | 컬러 입력 | 비고 |
|------|--------|--------|----------|------|
| 브랜드형 | `bannerColor` (사용자 지정) | `bannerText` (maxlength 20) | ✅ 컬러피커 | 기본값 `#00A1FF` |
| 오마트 | `#ff8442` (고정) | `bannerText` (maxlength 20) | ❌ | 브랜드 컬러 고정 |
| 오세일 | `bannerColor` (사용자 지정) | `bannerText` (maxlength 20) | ✅ 컬러피커 | - |
| 오늘의딜 | `#FF4747` (고정) | `bannerText` (maxlength 20) | ❌ | 브랜드 컬러 고정 |
| 스오딜 | `#ff4747` (고정) | 탭 2~5개 (각 maxlength 5) | ❌ | 활성 탭 노란색 강조 |
| 없음 | - | - | - | 띠배너 미표시 |

#### 띠배너 공통 스타일
- 높이: `104px`
- 좌우 패딩: 일반 `40px` / 스오딜 `30px`
- info 아이콘: 좌측, `width/height: 36px`, opacity 0.7 (스오딜 제외)
- 텍스트 컬러: white

#### 스오딜 탭 스타일
- 각 탭: `flex: 1`, 정렬 가운데
- 활성 탭:
  - 텍스트 컬러: `#fbff00` (노란색)
  - font-weight: 600
  - 하단 보더: `4px solid #fbff00`
- 비활성 탭:
  - 텍스트 컬러: white
  - font-weight: 400

---

### 배지 종류 & 규칙

#### 배지 카테고리
- **무결제계열** (5종): 즉시할인, 상품쿠폰, 장바구니, 무료배송, 사용자기재
- **결제사계열** (4종): 결제사_카카오페이, 결제사_네이버페이, 결제사_토스페이, 결제사_오늘의집페이

#### 배지 개수별 사용 규칙

| 개수 | 1번 | 2번 | 3번 |
|------|-----|-----|-----|
| 1개 | 모든 종류 가능 | - | - |
| 2개 | 무결제계열만 | 모든 종류 가능 | - |
| 3개 | 무결제계열만 | 무결제계열만 | 결제사계열만 |
| 없음 | - | - | - |

#### 결제사 배지 색상 (`_payC`)

| 결제사 | 배경색 | 텍스트 색상 | 비고 |
|--------|--------|------------|------|
| 카카오페이 | `#ffeb00` (노랑) | `#2f2f2f` (다크) | 텍스트 가독성 |
| 네이버페이 | `#00DE5A` (초록) | `#2f2f2f` (다크) | - |
| 토스페이 | `#3182F6` (블루) | `#ffffff` (화이트) | - |
| 오늘의집페이 | `#00A1FF` (스카이) | `#ffffff` (화이트) | - |

#### 배지 박스 공통 스타일
- 크기: `width: 160px × height: 160px`
- 모양: 원형 (`border-radius: 200px`)
- 패딩: `30px` (일반/결제사 토스페이) / `25px 30px 30px` (다른 결제사)
- 정렬: `flex-direction: column`, `align-items: center`, `justify-content: center`, `gap: 4px`
- 텍스트 정렬: 가운데

#### 일반 배지 배경
- 기본: `#2F2F2F` (다크 그레이)
- "배지 밝게" 토글 ON 시: `#3F3F3F` (살짝 밝게, 이미지 어두울 때 가독성 보조)

---

### 타이포그래피

#### 띠배너 텍스트

| 요소 | 폰트 크기 | 굵기 | letter-spacing | 색상 |
|------|----------|------|----------------|------|
| 일반 띠배너 텍스트 | 30px | 400 | -0.33px | white |
| 스오딜 탭 (활성) | 30px | 600 | -0.5px | `#fbff00` |
| 스오딜 탭 (비활성) | 30px | 400 | -0.5px | white |

#### 배지 텍스트 (재정비된 명세 ⭐)

| 배지 종류 | 1줄 | 2줄 |
|----------|-----|-----|
| **일반 배지** (즉시할인 등) | 30px / weight 동적 / opacity 0.9 / -0.3px | 26px / weight 400 / `rgba(255,255,255,0.6)` / line-height 28px |
| **결제사 배지** | 30px / weight 동적 / opacity 0.9 / -0.3px | 26px / weight 400 / 동적 컬러 / line-height 29px |
| **무료배송** ⭐ | **28px** / weight 500 / white opacity 0.9 / -0.3px | **28px** / weight 500 / white opacity 0.9 / -0.3px |
| **사용자기재** ⭐ | **28px** / weight 500 / white opacity 0.9 / -0.26px / line-height 1.2 | **26px** / weight 400 / `rgba(255,255,255,0.6)` / -0.26px / line-height 28px |

**스타일 일관성 의도**:
- 무료배송: "무료/배송"이 한 단어처럼 보이도록 두 줄 동일 (한글 시각 보정 위해 28px)
- 사용자기재 2줄: 일반 배지 2줄과 동일한 보조 텍스트 스타일

#### 결제사 배지 텍스트 색상 분기 (코드 변수)
- `rateTc` (1줄): 토스페이/오늘의집페이 → `#ffffff`, 카카오페이/네이버페이 → `#2f2f2f`
- `nameTc` (2줄): 토스페이/오늘의집페이 → `rgba(255,255,255,0.6)`, 카카오페이/네이버페이 → `rgba(47,47,47,0.7)`
- `payPn`: 결제사 이름 4자 이상 시 "OO<br>페이" 줄바꿈 (예: "카카오<br>페이")

#### 하단 유의사항
- 폰트 크기: 26px
- 굵기: 300 (Light)
- 색상: `rgba(255,255,255,0.7)`
- letter-spacing: -0.26px
- 정렬: 가운데
- maxlength: 30
- 기본 텍스트: "할인 혜택은 조기 종료될 수 있음"

---

### 색상

#### 배지 배경
- 일반 배지: `#2F2F2F` (기본) / `#3F3F3F` (배지 밝게 ON)
- 무료배송: `#2F2F2F` (`_badgeBg` 변수)
- 사용자기재: `#2F2F2F` (`_badgeBg` 변수)
- 결제사: 결제사별 브랜드 컬러 (위 표 참조)

#### 띠배너
- 브랜드형/오세일: 사용자 지정 (`bannerColor`)
- 오마트: `#ff8442` (고정)
- 오늘의딜/스오딜: `#FF4747` (고정)

#### 기타
- 카드 자체 배경: 없음 (이미지가 배경 역할)
- 빈 이미지 상태: `#E8E8E4`
- 빈 이미지 텍스트: `#C0C0BC` (24px)

---

### 인터랙션

| 기능 | 지원 | 비고 |
|------|------|------|
| 이미지 위치 조정 | ✅ | 드래그/핸들/휠 (X, Y, Scale) |
| 자동 컬러 추출 | ❌ | 배지·띠배너 색상 모두 고정 또는 수동 |
| 스포이드 (이미지 컬러 추출) | ✅ | 띠배너 컬러피커에서 가능 |
| 배지 개수 조정 | ✅ | 없음 / 1개 / 2개 / 3개 |
| 배지 종류 선택 | ✅ | 개수별 사용 규칙 적용 |
| 배지 밝게 토글 | ✅ | `darkImg` (이미지 어두울 때 배지 살짝 밝게) |
| 유의사항 표시 토글 | ✅ | `showNotice` (on/off) |
| 유의사항 텍스트 편집 | ✅ | `noticeText` (maxlength 30) |
| 띠배너 타입 변경 | ✅ | 6종 + 각자의 입력 필드 동적 표시 |
| 스오딜 탭 개수 조정 | ✅ | 2/3/4/5개, 활성 탭 선택 가능 |

#### 입력 검증
- **할인율**: 숫자만 입력 (정규식 `[^0-9]` 제거), 최대 3자리 + `%` 자동 부착
- **사용자기재 1줄/2줄**: maxlength 5
- **스오딜 탭**: maxlength 5
- **배너 텍스트**: maxlength 20
- **유의사항**: maxlength 30

---

### 데이터 구조 (default)

```javascript
{
  // 띠배너
  bannerType: '브랜드형',         // '브랜드형' | '오마트' | '오세일' | '오늘의딜' | '스오딜' | '없음'
  bannerText: '브랜드명',          // maxlength 20
  bannerColor: '#00A1FF',          // 브랜드형/오세일 전용
  
  // 스오딜 탭 (bannerType === '스오딜'일 때만 사용)
  tab1: '스페셜딜',
  tab2: '특가더보기',
  tab3: '라이브',
  tab4: '오마트',
  tab5: '콜라보마켓',
  tabCount: '5',                   // '2' | '3' | '4' | '5'
  activeTab: '1',                  // '1' ~ tabCount
  
  // 배지
  badgeCount: '3개',               // '1개' | '2개' | '3개' | '없음'
  badge1Type: '즉시할인',           // 무결제계열 또는 결제사계열
  badge1Rate: '57%',               // 할인율 (자동 % 부착)
  badge1Line1: '혜택1',            // 사용자기재 1줄 (maxlength 5)
  badge1Line2: '',                 // 사용자기재 2줄 (maxlength 5)
  badge2Type: '상품쿠폰',
  badge2Rate: '15%',
  badge2Line1: '혜택2',
  badge2Line2: '',
  badge3Type: '결제사_카카오페이',
  badge3Rate: '7%',
  badge3Line1: '혜택3',
  badge3Line2: '',
  
  // 유의사항
  showNotice: 'on',                // 'on' | 'off'
  noticeText: '할인 혜택은 조기 종료될 수 있음',
  
  // 이미지 & 토글
  _img: '',
  darkImg: false,                  // 배지 밝게 토글
  _imgX: 0, _imgY: 0, _imgScale: 1,
  _imgEditing: false
}
```

---

## 🎨 카드 D ~ K — 작성 예정

D~K 카드는 현재 범용 fields 폼만 사용 중이며, 커스텀 에디터 작업 시 명세 추가 예정.

| 카드 | 타입 | 위치 | 커스텀 에디터 | 명세 작성 |
|------|------|------|--------------|----------|
| D | 제품리스트형 | 내지 | ❌ | 추후 |
| E | (미정) | - | ❌ | 추후 |
| F | (미정) | - | ❌ | 추후 |
| G | 이미지형 | 내지 | ❌ | 추후 |
| H | 프레임형 | 내지 | ❌ | 추후 |
| H1 | 룩북형 (2컷) | 내지 | ❌ | 추후 |
| H2 | 룩북형 (3컷) | 내지 | ❌ | 추후 |
| H3 | 룩북형 (4컷) | 내지 | ❌ | 추후 |
| I | 자유형 | 내지 | ❌ | 추후 |
| J | 카테고리형 | 아웃트로 | ❌ | 추후 |
| K | (미정) | - | ❌ | 추후 |

---

## 📚 참고 문서

- `COMPONENT_RULES.md` — 빌더 UI 공통 컴포넌트 규칙 (캐러셀과 캐러셀 와이드 공통)
- `TEMPLATE_CHECKLIST.md` — 새 카드 만들 때 체크리스트
- `TODO_NEXT.md` — 다음 작업 목록
- `CLAUDE.md` — 프로젝트 개요

---

## 📝 명세서 작성 가이드 (내부)

새 카드 작업 또는 기존 카드 수정 시:
1. 해당 카드 섹션의 "작성 예정" 부분을 채운다
2. 픽셀 값은 코드와 일치하도록 정확히 기록
3. 색상은 hex 코드로 표기
4. 인터랙션은 어떤 옵션이 있는지 명시
5. 변형(예: 1개/2개, 누끼/연출컷)이 있으면 각각 작성
